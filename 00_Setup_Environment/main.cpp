# include <iostream>

int main(int argc, char const *argv[]) {

    // Setup the Neovim and Compiler to start C++ 20.
    // Compiler = G++;
    // I'm using C++20;

    // Compile Command = g++ filename.cpp -std=c++20 -o filename && ./filename

    return 0;
}
