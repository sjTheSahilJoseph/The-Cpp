#include <cmath>
#include <iostream>

int main(int argc, char const* argv[]) {
    // Math Functions.
    // We can also perform some math functions which are defined in cmath header
    // file.

    // We can do several things and there are so many functions.

    return 0;
}
