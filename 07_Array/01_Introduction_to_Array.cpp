#include <iostream>

int main(int argc, char const* argv[]) {
    std::cout << "Array" << std::endl;

    /*
       Array.
       Array is the collection of variables of same data types.
       We can store multiple data into a single array.
       And then we can index it by 0.

       // Array is contiguous blocks of memory.
       // For example if we have an array of 4 integers. then these 4 integers
       will be stored contiguously in the Memory.
       // We can also go to the next element by pointer incrementation.

       // Array's index starts form 0.

    */

    return 0;
}
