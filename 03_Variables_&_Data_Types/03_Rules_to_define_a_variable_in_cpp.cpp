int main(int argc, char const *argv[]) {

  // Rules to define a variable in C++.
  // Variable names are case sensitive.
  // Variable name must starts with a letter or with underscore.
  // We can't use any other symbol in our variable name. (start,end,between).
  // No spaces or special characters are allowed in variable names.

  // General syntax.
  // type_name variable_name = value;
  int variableName = 3;

  return 0;
}
